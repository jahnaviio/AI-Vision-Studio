# ---------------------------------------------------------------------------
# AI Vision Studio — Python dependencies
# ---------------------------------------------------------------------------
# Tested with Python 3.10 / 3.11.
# Install with:  pip install -r requirements.txt

# ---- Interface ----
gradio>=4.36.0,<5.0.0

# ---- Core CV / numerics ----
opencv-python-headless>=4.9.0
numpy>=1.24,<2.0
Pillow>=10.0.0

# ---- Face Analysis (OpenCV + DeepFace) ----
deepface>=0.0.93
tf-keras>=2.16.0
tensorflow-cpu>=2.16.0,<2.17.0      # swap for `tensorflow` if you have a GPU build configured

# ---- Object Detection ----
ultralytics>=8.2.0                  # YOLO-World (default real-time engine)
torch>=2.2.0                        # used by YOLO-World, Grounding DINO, Florence-2
torchvision>=0.17.0
transformers>=4.41.0                # Grounding DINO + Florence-2
timm>=1.0.0                         # Florence-2 vision backbone
einops>=0.7.0                       # Florence-2
accelerate>=0.30.0
